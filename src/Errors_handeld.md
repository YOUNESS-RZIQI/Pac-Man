# Errors Handeld:

- 1. No Permession to `configuration.jon`

- 2. `configuration.jon` Not a JSON File

- 3. No `configuration.jon` gaved in the Input

- 4. Loading `configuration.jon` in get_config (print and exit)


# Just Wornings:

- 1. If `Game Data` it sell not Exist or not a Dict.  

- 2. For Invalid `Game Data Keys Value` like ("width","height","lives","pacgum","points_per_pacgum" ...).